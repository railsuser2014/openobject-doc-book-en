
.. i18n: Shutting down the server 
.. i18n: ======================== 

Shutting down the server 
======================== 

.. i18n: The easiest way the shut down the server on a Linux type system is to send the SIG INT signal to the server.

The easiest way the shut down the server on a Linux type system is to send the SIG INT signal to the server.

.. i18n:     * Find the Process ID.
.. i18n:           - # ps ax | grep -i tiny 
.. i18n:     * Use the kill comand with the PID.
.. i18n:           - # kill -2 pid 

    * Find the Process ID.
          - # ps ax | grep -i tiny 
    * Use the kill comand with the PID.
          - # kill -2 pid 
